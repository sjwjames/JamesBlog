import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class LiteralComponent extends Component {
    render() {
        const { } = this.props;

        return (
            <div>Literature</div>
        )
    }
}

LiteralComponent.propTypes = {

};